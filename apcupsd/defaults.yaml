# -*- coding: utf-8 -*-
# vim: ft=yaml
---
apcupsd:
  pkg:
    name: apcupsd
  rootgroup: root
  config: '/etc/apcupsd'
  service:
    name: apcupsd
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
